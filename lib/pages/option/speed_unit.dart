import 'package:flutter/material.dart';

class SpeedUnitPage extends StatelessWidget {
  const SpeedUnitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Speed Unit "),
      ),
    );
  }
}
